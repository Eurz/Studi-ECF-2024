<form method="post" action="{{ path('admin_testimonials_delete', {'id': testimonial.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ testimonial.id) }}">
	<button class="btn btn-sm btn-danger">Supprimer</button>
</form>
