{% extends 'base.html.twig' %}

{% block title %}Editer un véhicule
{% endblock %}

{% block body %}
	<h1>{{ page_title }}</h1>
	<a href="{{ path('admin_vehicles_index') }}">Retour</a>

	{{ include('admin/vehicles/_form.html.twig') }}

{# {{ include('admin/services/_form.html.twig', {'button_label': 'Mettre à jour'}) }} #}

	{# {{ include('admin/services/_delete_form.html.twig') }} #}
{% endblock %}
