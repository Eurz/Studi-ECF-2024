{% extends 'base.html.twig' %}

{% block title %}
	{{ pageTitle }}
{% endblock %}

{% block body %}
	<h1>{{ pageTitle }}</h1>

	<div>
		<a href="{{ path('admin_users_index') }}">Retour</a>
	</div>
	{{ include('admin/user/_form.html.twig') }}

{% endblock %}
